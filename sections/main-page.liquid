<div class="gm-section gm-container" style="max-width: 90rem;">
  <span class="gm-eyebrow">{{ 'La Maison' }}</span>
  <h1 class="gm-h2">{{ page.title }}</h1>
  <div class="gm-lead" style="margin-top: var(--space-5); color: var(--gm-text);">
    {{ page.content }}
  </div>
</div>

{% schema %}
{
  "name": "t:sections.main-page.name",
  "settings": []
}
{% endschema %}
